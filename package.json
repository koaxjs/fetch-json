{
  "name": "@koax/fetch-json",
  "description": "Fetch json sugar.",
  "repository": "git://github.com/koaxjs/fetch-json.git",
  "version": "0.1.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "prepublish": "make build",
    "postpublish": "make clean",
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@f/is-object": "^1.1.4",
    "@koax/fetch": "^0.1.2"
  },
  "devDependencies": {
    "babel-cli": "^6.4.0",
    "babel-preset-es2015": "^6.3.13",
    "babel-register": "^6.3.13",
    "babel-polyfill": "^6.3.13",
    "tape": "^4.2.2"
  }
}
